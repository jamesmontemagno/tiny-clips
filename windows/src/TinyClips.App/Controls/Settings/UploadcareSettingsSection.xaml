<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TinyClips.App.Settings.Sections.UploadcareSettingsSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Border HorizontalAlignment="Stretch">
        <StackPanel
            MaxWidth="1064"
            HorizontalAlignment="Stretch"
            Spacing="4">
            <StackPanel
                Margin="4,8,4,12"
                Spacing="2">
                <TextBlock
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Text="Uploadcare" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="Tiny Clips uploads directly to your own Uploadcare project. It does not include credentials or manage your Uploadcare account."
                    TextWrapping="Wrap" />
                <HyperlinkButton
                    HorizontalAlignment="Left"
                    Padding="0,4"
                    AutomationProperties.AutomationId="UploadcareApiKeysLink"
                    AutomationProperties.Name="Create an Uploadcare account or find API keys"
                    Content="Create an account or find your API keys"
                    FontSize="12"
                    NavigateUri="https://app.uploadcare.com/projects/-/api-keys/" />
            </StackPanel>

            <StackPanel Spacing="2">
                <controls:SettingsCard
                    Description="Upload saved captures to your Uploadcare project."
                    Header="Enable Uploadcare"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE898;}">
                    <ToggleSwitch
                        AutomationProperties.AutomationId="UploadcareEnabledToggle"
                        AutomationProperties.Name="Enable Uploadcare uploads"
                        IsOn="{x:Bind ViewModel.UploadcareEnabled, Mode=TwoWay}" />
                </controls:SettingsCard>

                <controls:SettingsCard
                    Description="Required for uploads. This key is saved with your non-secret Tiny Clips settings."
                    Header="Public key"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE8D7;}">
                    <TextBox
                        MinWidth="240"
                        MaxWidth="420"
                        AutomationProperties.AutomationId="UploadcarePublicKeyTextBox"
                        AutomationProperties.Name="Uploadcare public key"
                        PlaceholderText="demopublickey"
                        Text="{x:Bind ViewModel.UploadcarePublicKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </controls:SettingsCard>

                <controls:SettingsCard
                    Header="Secret key (optional)"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE72E;}">
                    <controls:SettingsCard.Description>
                        <StackPanel>
                            <TextBlock
                                Text="Used only to sign Uploadcare requests. It is stored in Windows Credential Locker, never in Tiny Clips settings."
                                TextWrapping="Wrap" />
                            <TextBlock
                                Margin="0,4,0,0"
                                Text="{x:Bind ViewModel.UploadcareSecretKeyStatus, Mode=OneWay}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </controls:SettingsCard.Description>
                    <Grid
                        MinWidth="320"
                        MaxWidth="520"
                        ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <PasswordBox
                            x:Name="SecretKeyBox"
                            AutomationProperties.AutomationId="UploadcareSecretKeyPasswordBox"
                            AutomationProperties.Name="Uploadcare secret key"
                            PasswordRevealMode="Peek"
                            PlaceholderText="Enter a new secret key" />
                        <Button
                            Grid.Column="1"
                            AutomationProperties.AutomationId="SaveUploadcareSecretKeyButton"
                            Click="OnSaveSecretKey"
                            Content="Save securely" />
                        <Button
                            Grid.Column="2"
                            AutomationProperties.AutomationId="ClearUploadcareSecretKeyButton"
                            Click="OnClearSecretKey"
                            Content="Clear" />
                    </Grid>
                </controls:SettingsCard>
            </StackPanel>

            <TextBlock
                Margin="4,16,0,0"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="After saving a capture" />

            <StackPanel Spacing="2">
                <controls:SettingsCard
                    Description="Upload after Tiny Clips successfully finishes saving a screenshot, video, or GIF."
                    Header="Upload automatically"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE898;}">
                    <ToggleSwitch
                        AutomationProperties.AutomationId="UploadcareAutoUploadToggle"
                        AutomationProperties.Name="Upload automatically after saving"
                        IsOn="{x:Bind ViewModel.UploadcareAutoUpload, Mode=TwoWay}" />
                </controls:SettingsCard>

                <controls:SettingsCard
                    Description="Copy the Uploadcare delivery URL when an automatic upload succeeds."
                    Header="Copy uploaded URL"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE8C8;}">
                    <ToggleSwitch
                        AutomationProperties.AutomationId="UploadcareCopyUrlToggle"
                        AutomationProperties.Name="Copy uploaded URL"
                        IsOn="{x:Bind ViewModel.UploadcareCopyUrl, Mode=TwoWay}" />
                </controls:SettingsCard>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
