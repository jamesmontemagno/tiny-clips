<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TinyClips.App.Settings.Sections.TeleprompterSettingsSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Border HorizontalAlignment="Stretch">
        <StackPanel
            MaxWidth="1064"
            HorizontalAlignment="Stretch"
            Spacing="8">
            <TextBlock
                Margin="4,8,0,4"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="Teleprompter" />

            <controls:SettingsCard
                Description="Show an auto-scrolling transcript overlay while recording video. The overlay is never captured."
                Header="Teleprompter overlay"
                HeaderIcon="{ui:FontIcon Glyph=&#xE8F1;}">
                <ToggleSwitch
                    VerticalAlignment="Center"
                    AutomationProperties.AutomationId="TeleprompterEnabledToggle"
                    AutomationProperties.Name="Enable teleprompter overlay"
                    IsOn="{x:Bind ViewModel.TeleprompterEnabled, Mode=TwoWay}" />
            </controls:SettingsCard>

        <Border Style="{StaticResource SettingCardStyle}">
            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Transcript" />
                <TextBox
                    MinHeight="160"
                    AcceptsReturn="True"
                    AutomationProperties.AutomationId="TeleprompterTranscriptTextBox"
                    AutomationProperties.Name="Teleprompter transcript"
                    IsEnabled="{x:Bind ViewModel.TeleprompterEnabled, Mode=OneWay}"
                    PlaceholderText="Paste or type the script to scroll during video recordings…"
                    Text="{x:Bind ViewModel.TeleprompterTranscript, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Shown for video recordings only — never for GIFs or screenshots. The overlay stays hidden while this is empty."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Style="{StaticResource SettingCardStyle}">
            <StackPanel Spacing="8">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="Scroll speed" />
                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{x:Bind ViewModel.TeleprompterScrollSpeedDisplay, Mode=OneWay}" />
                </Grid>
                <Slider
                    AutomationProperties.AutomationId="TeleprompterScrollSpeedSlider"
                    AutomationProperties.Name="Teleprompter scroll speed"
                    IsEnabled="{x:Bind ViewModel.TeleprompterEnabled, Mode=OneWay}"
                    Maximum="200"
                    Minimum="10"
                    Value="{x:Bind ViewModel.TeleprompterScrollSpeed, Mode=TwoWay}" />
            </StackPanel>
        </Border>

            <Border Style="{StaticResource SettingCardStyle}">
                <StackPanel Spacing="8">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Preview" />
                        <TextBlock
                            Grid.Column="1"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind ViewModel.TeleprompterScrollSpeedDisplay, Mode=OneWay}" />
                    </Grid>
                    <Border
                        Height="140"
                        Padding="16,12"
                        AutomationProperties.HelpText="Shows how quickly the transcript will move during a recording."
                        AutomationProperties.Name="Teleprompter scroll speed preview"
                        Background="#99000000"
                        CornerRadius="{StaticResource OverlayCornerRadius}">
                        <ScrollViewer
                            x:Name="PreviewScroller"
                            HorizontalScrollBarVisibility="Disabled"
                            HorizontalScrollMode="Disabled"
                            IsHitTestVisible="False"
                            SizeChanged="OnPreviewSizeChanged"
                            VerticalScrollBarVisibility="Hidden"
                            VerticalScrollMode="Disabled"
                            ZoomMode="Disabled">
                            <TextBlock
                                x:Name="PreviewText"
                                FontSize="24"
                                Foreground="#FFFFFFFF"
                                TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                    <Button
                        x:Name="PreviewToggleButton"
                        HorizontalAlignment="Left"
                        AutomationProperties.AutomationId="TeleprompterPreviewToggleButton"
                        AutomationProperties.Name="Start teleprompter preview"
                        Click="OnPreviewToggleClicked"
                        Content="Start Preview"
                        IsEnabled="{x:Bind ViewModel.TeleprompterEnabled, Mode=OneWay}" />
                    <TextBlock
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Start the preview to test the same scroll speed used by the recording overlay."
                        TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
