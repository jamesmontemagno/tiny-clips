<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TinyClips.App.RecordingIndicatorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid
        x:Name="RootGrid"
        Background="Transparent"
        PointerCanceled="OnPointerCaptureEnded"
        PointerCaptureLost="OnPointerCaptureEnded"
        PointerMoved="OnPointerMoved"
        PointerPressed="OnPointerPressed"
        PointerReleased="OnPointerReleased">
        <Border
            Padding="12,8"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource OverlayCornerRadius}">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Ellipse
                    Width="12"
                    Height="12"
                    VerticalAlignment="Center"
                    Fill="{ThemeResource SystemFillColorCriticalBrush}" />

                <StackPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="2">
                    <TextBlock
                        x:Name="ElapsedText"
                        AutomationProperties.LiveSetting="Polite"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="00:00" />
                    <TextBlock
                        x:Name="HotKeyText"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Stop from tray" />
                </StackPanel>

                <StackPanel
                    Grid.Column="2"
                    Orientation="Horizontal"
                    Spacing="6">
                    <Button
                        x:Name="PauseButton"
                        AutomationProperties.AutomationId="PauseRecordingButton"
                        AutomationProperties.Name="Pause recording"
                        Click="OnPauseClick"
                        Content="Pause"
                        ToolTipService.ToolTip="Pause recording" />
                    <Button
                        x:Name="ResumeButton"
                        AutomationProperties.AutomationId="ResumeRecordingButton"
                        AutomationProperties.Name="Resume recording"
                        Click="OnResumeClick"
                        Content="Resume"
                        ToolTipService.ToolTip="Resume recording"
                        Visibility="Collapsed" />
                    <ToggleButton
                        x:Name="SystemAudioButton"
                        AutomationProperties.AutomationId="SystemAudioMuteButton"
                        AutomationProperties.Name="System audio"
                        Click="OnSystemAudioClick"
                        ToolTipService.ToolTip="System audio"
                        Visibility="Collapsed">
                        <FontIcon x:Name="SystemAudioIcon" Glyph="&#xE767;" />
                    </ToggleButton>
                    <ToggleButton
                        x:Name="MicrophoneButton"
                        AutomationProperties.AutomationId="MicrophoneMuteButton"
                        AutomationProperties.Name="Microphone"
                        Click="OnMicrophoneClick"
                        ToolTipService.ToolTip="Microphone"
                        Visibility="Collapsed">
                        <FontIcon x:Name="MicrophoneIcon" Glyph="&#xE720;" />
                    </ToggleButton>
                    <Button
                        x:Name="RestartButton"
                        AutomationProperties.AutomationId="RestartRecordingButton"
                        AutomationProperties.Name="Restart recording"
                        Click="OnRestartClick"
                        Content="Restart"
                        ToolTipService.ToolTip="Discard this take and immediately start over" />
                    <Button
                        x:Name="DiscardButton"
                        AutomationProperties.AutomationId="DiscardRecordingButton"
                        AutomationProperties.Name="Discard recording"
                        Click="OnDiscardClick"
                        Content="Discard"
                        ToolTipService.ToolTip="Discard recording" />
                    <Button
                        x:Name="StopButton"
                        AutomationProperties.AutomationId="StopRecordingButton"
                        AutomationProperties.Name="Stop recording"
                        Click="OnStopClick"
                        Content="Stop"
                        Style="{StaticResource AccentButtonStyle}"
                        ToolTipService.ToolTip="Stop recording" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
